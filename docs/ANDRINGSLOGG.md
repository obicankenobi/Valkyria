# Ändringslogg — design och spec

En rad per beslutad ändring i `docs/DESIGN.md` eller `docs/ETAPP1_TEKNISK_SPEC.md`. Byggs
framåtriktat. **Ändra aldrig en gammal rad** — ett omprövat beslut får en ny.

Loggen finns för att planen är avsiktligt flexibel. Flexibilitet utan spår blir drift, och drift
är det som gör att ingen längre vet vilken version av reglerna som faktiskt gäller.

Regeln: en ändrad spec utan rad här är samma sak som ingen ändring.

| Datum | Dokument | Ändring | Varför |
|---|---|---|---|
| 2026-09-13 | DESIGN v2.0 → v2.1, SPEC v1.0 → v1.1 | Styckkostnad och COGS införda; `supplyCostIndex` verkar på kostnad i stället för pris | `grade C` var strikt sämre än A och spelaren hade ingen marginal att räkna på |
| 2026-09-13 | DESIGN, SPEC | `QUOTE` kostar inte längre en executive action; knappheten flyttad till produktionskapacitet och `reliability` | Anbud var enda intäktskällan, så alla tre handlingar gick åt till att bjuda och turvalet var redan fattat |
| 2026-09-13 | SPEC | `referencePrice` fryses när ordern skapas | Räknades tidigare om i `bidding`, efter att `heat` hunnit röra sig i samma tur — spelaren bjöd mot ett tal som flyttade sig |
| 2026-09-13 | SPEC | `trueBudget` diskvalificerar bud över taket | Fältet var oanvänt, vilket gjorde `statedBudget`-lögnen betydelselös |
| 2026-09-13 | DESIGN, SPEC | `creditLimit` införd, `TAKE_LOAN` prövas mot tak | `INSOLVENCY` var trivialt undvikbar genom att låna vidare i all oändlighet |
| 2026-09-13 | DESIGN, SPEC | Styrelsens prognoskontroller vid tur 8 och 14; `BUYOUT` vid två underkända i rad | `BUYOUT` kunde annars bara inträffa på scenariots sista tur, så passivitet kändes aldrig under tiden |
| 2026-09-13 | DESIGN, SPEC | `heat` omkalibrerad; avsvalning bara turer utan leverans in i aktiv konflikt | Design och spec angav olika regel, och specens konstanter krävde 200 enheter per tur bara för att stå stilla |
| 2026-09-13 | DESIGN, SPEC | `restricted`-produkter med `doomsdayOnDelivery` införda i etapp 1 | Etapp 1 saknade varje `DOOMSDAY`-drivare utom iscensatta incidenter och kunde alltså inte pröva designpelare 1 |
| 2026-09-13 | SPEC | Wire-beskärning behåller orsakskedjor; invarianttest tillagt | En kedja som tar slut mitt i ser ut som att spelet döljer något |
| 2026-09-13 | SPEC | Golden-testet läser `balance.frozen.json` i stället för den levande balansfilen | P10 skruvar `balance.json` hundratals gånger medan `CLAUDE.md` kräver stopp vid brutet snapshot — kraven hade dödat varandra |
| 2026-09-13 | SPEC | P4:s acceptanstest utbytt mot fyra icke-triviala krav | Det gamla ("vinstchansen monoton i pris") följer av formelns konstruktion och kunde inte misslyckas |
| 2026-09-13 | DESIGN, SPEC | `INDOCHINA_SLICE` är ett eget 20-turersscenario, inte en avkortning av `INDOCHINA` (34 turer) | Det fulla scenariots femårsmål går inte att pröva i en kortare körning |
| 2026-09-13 | DESIGN, SPEC | `ASSASSINATE` uttryckligen flyttad till etapp 2 | Fanns i designdokumentet men saknades i `IntelOp` — otydligheten hade byggts av misstag |
| 2026-09-13 | SPEC | Invarianttester för penningavrundning och orderlivslängd tillagda | `Money` säger "hela £" men ränta producerar flyttal; en nygenererad order kunde avgöras samma tur den föddes |
| 2026-09-13 | SPEC | `bidEstimate` returnerar `winBand` (pris → vinstsannolikhet) och `yourUnitCost` | En exakt rivalprissiffra avgör ändå inte anbudet och ser ut som ett facit utan att vara det |
| 2026-09-13 | SPEC | `StandingOrderChange` (i `TurnSubmission`) fick en minimal, uttryckligt provisorisk platshållarform (`{ kind: string; payload: Record<string, unknown> }`) i `types.ts` — **öppen fråga, inte ett beslut.** Formen namngavs i spec 3 men definierades aldrig, varken där eller i DESIGN.md | Blockerade P2:s bygge av `TurnSubmission` rent typmässigt. `applyActions` är no-op i P2 och läser aldrig innehållet, så platshållaren har ingen spelmässig effekt än — men den riktiga formen (sannolikt en diskriminerad union per DESIGN.md §4: produktionslinjer, R&D-kö, leverantörsavtal, prisgolv, stationers underhållsläge) måste beslutas innan en prompt faktiskt bearbetar standing orders |
| 2026-09-13 | SPEC | Noterat (ingen kodändring): ingen prompt i avsnitt 10 äger `applyActions` för riktig handlings-/budbearbetning, till skillnad från `orders`→P4, `fronts`→P6 osv. | Upptäckt under P2:s genomläsning av promptsekvensen. `applyActions` förblir no-op i P2 som alla andra tolv steg, men saknar en framtida hemprompt — måste tilldelas innan P4/P5 (bud, kontrakt) blir meningsfulla |
| 2026-09-13 | SPEC | `alignmentPenalty(faction.alignment, house)` (avsnitt 4.4) fick en minimal, uttryckligt provisorisk formel i `pricing.ts`: skalad mot husets west-/eastStanding, noll för en helt neutral faktion, storleksordning kalibrerad mot repTerm. **Öppen fråga, inte ett beslut** — en riktig formel är en designfråga | Namnges i poängformeln men definieras aldrig, varken formel eller vad `house`-argumentet bidrar med. Blockerar inget av P4:s fem klart när-villkor (verifierat oberoende för var och en), så en provisorisk platshållare kunde byggas utan att stanna hela prompten |
| 2026-09-13 | SPEC | "Rivaler poängsätts med samma formel men med relationTerm och repTerm från deras egna värden" (avsnitt 4.4) löst genom att rivaler får relationTerm 0, repTerm 0, bribeTerm 0, blocTerm 0 — de konkurrerar bara på pris och leveranstid | `RivalHouse` (avsnitt 2.5) har inga rykte/relations-fält att hämta "egna värden" ur. Det avsnitt 4.2:s rivalbudsformel faktiskt modellerar för en rival är pris och leveranstid, så det är den enda delen av 4.4:s formel som meningsfullt kan tillämpas på dem |
| 2026-09-13 | SPEC | Ordergenereringens kadens (hur ofta, vilka köpare, vilka kvantiteter) fick en minimal, uttryckligt provisorisk regel i `orders.ts`: en chans per faktion och tur (`orderGenerationChancePct`), kvantitet och budgetfaktorer i nya balance.json-fält. **Öppen fråga** — P10:s balanspass förväntas skruva/ersätta den | Specen ger prissättningsformeln för en redan beslutad order (avsnitt 4.1) men aldrig hur ofta eller vilka köpare som utlyser en |
| 2026-09-13 | SPEC | `core/src/pricing.ts` tillagd — inte namngiven i avsnitt 1:s filträd | Delar formlerna i avsnitt 4 mellan `orders.ts`, `bidding.ts` och `queries.ts`. `bidEstimate` måste approximera exakt vad `bidding.ts` gör (winBands hela poäng, 4.3) — två handkopierade implementationer hade varit en tyst driftrisk |
| 2026-09-13 | SPEC | Balansdata utökad med ~20 nya fält i `balance.json` (scarcityPriceDivisor, rivalMargin*, score-vikterna, relationBoost*, orderGeneration* m.fl.) | Krävs av formlerna i avsnitt 4.1/4.2/4.4, som uttryckligen säger "Detta är siffrorna att skruva på först. De ligger i balance.json, inte i koden" (avsnitt 4.4) — CLAUDE.md hård regel 5 |
| 2026-09-13 | SPEC | `Shipment` (i `market.shipments`) tillagd i `types.ts` — inte i avsnitt 2 | "Leveranser anländer 1–3 turer efter produktionen" (avsnitt 5) har ingen egen datamodell i den frysta specen. `Contract.unitsDelivered` räcker inte ensamt för att skilja producerat från levererat när de två förskjuts i tiden |
| 2026-09-13 | SPEC | `doomsdayGate.ts` byggd i P5, inte "P7" | Min egen preliminära notis i P0-skelettet gissade P7 — den gissningen var aldrig auktoritativ spec, bara min egen uppskattning. P5:s instruktionstext kräver uttryckligen att restricted-leverans anropar doomsdayGate. `resolve/steps/doomsday.ts` (avkylning, trösklar, krisevent) förblir P7:s jobb, oförändrat |
| 2026-09-13 | SPEC | Noterat (ingen kodändring): `gradeScandalChance` i balance.json (satt i P1) konsumeras fortfarande ingenstans | "Quality scandal" (DESIGN.md §5.1) nämns inte i P5:s instruktionstext trots att P5 är den prompt som annars äger grade-relaterade produktionskonsekvenser. Hör hemma i en framtida, ännu inte tilldelad prompt |
| 2026-09-13 | SPEC | `deliveries.ts` (P5, redan committad) utökad med "materiel in på front" + attribution-bokföring. **Nödvändig komplettering, inte en ny uppgift** | PIPELINE-kommentaren i avsnitt 3.2 sa alltid "leveranser anländer, betalning, materiel in på front" — men P5:s egen instruktionstext nämnde aldrig den tredje delen, så den byggdes aldrig. Utan den kan fronts.ts (P6) aldrig uppfylla sitt eget klart när-villkor |
| 2026-09-13 | SPEC | `Front.attribution`s "houseId"-nyckel löst med en reserverad konstant `PLAYER_ATTRIBUTION_KEY = 'player'` i deliveries.ts | `House` (till skillnad från `RivalHouse`) har inget `id`-fält — bara ett hus finns (spelarens eget), så attribution behövde ändå en stabil, icke-namnbaserad nyckel för det |
| 2026-09-13 | SPEC | **Hela `fronts.ts`-stridsmodellen** (avsnitt 5 "Front") är PROVISORISK utöver den enda givna formeln (`equipmentRatio`, artillerikategorin) — se `balance.json`s `_p6_note`. Manpowerfördel (analog till equipmentRatio, mot `strength`), terräng-/supplyStress-vägning, förlustfördelning, moralskifte, och tröskeldefinitionen för "genombrott" är alla mina konstruktioner. **Störst öppen fråga i projektet hittills — bör granskas separat, inte bara av P10:s balanspass** | Avsnitt 5:s Front-stycke är en enda mening lös prosa för allt utom equipmentRatio. Blockerade inget av P6:s tre klart när-villkor (verifierat: alla är riktnings-/summeringskrav, inga precisionskrav), så byggd med flit enkel och symmetrisk (bl.a. en explicit "båda sidor 0 materiel → fronten rör sig inte alls" så att stagnationskravet håller utan specialfall i själva formlerna) |
| 2026-09-13 | SPEC | `Faction` fick två nya räknare, `negativeTreasuryTurns`/`lowSupportTurns` — inte i avsnitt 2 | Avsnitt 5 kräver "`treasury < 0` i två turer"/"`publicSupport < 25` i tre turer" men gav `Faction` ingen motsvarighet till `House.insolventTurns`, som redan löser samma sorts krav för huset |
| 2026-09-13 | SPEC | `Theatre` fick en ny, självnollställande räknare `deliveriesIntoActiveWarThisTurn` — inte i avsnitt 2 | `heatFromDeliveries`-formeln (avsnitt 5, "Heat") behöver "denna turs leveranser in i aktiv konflikt". `deliveries.ts` fyller på den, `heat.ts` läser och nollställer den i samma pipelinepassage — enda sättet att flytta värdet mellan två separata steg utan att bryta `ResolveContext`s frysta form (avsnitt 3.2) |
| 2026-09-13 | SPEC | Embargo (`Faction.embargoed`) fick en PROVISORISK ekonomisk konsekvens i `factions.ts`: en kvartalsvis kassadränering (`embargoTreasuryDrainPerTurn`, nytt balance.json-fält). **Öppen fråga, inte ett beslut** | Avsnitt 5:s Faktion-stycke ger embargo ingen egen mekanik alls — bara fältet finns (avsnitt 2.5), och ingen `PlayerAction` kan sätta det (samma `applyActions`-lucka som loggats sedan P2). P7:s klart när-villkor kräver ändå en prövbar embargo → bankrutt → annullerat kontrakt-kedja |
| 2026-09-13 | SPEC | "`forced peace`" (avsnitt 5, `publicSupport < 25` i tre turer) implementerad som detektion + en engångsnotis, uttryckligen INGEN ytterligare mekanisk konsekvens (t.ex. frontinaktivering) | Varken avsnitt 5 eller DESIGN.md specificerar vad "forced peace" faktiskt gör utöver triggernamnet. Inget test kräver mer, och att hitta på en konsekvens hade varit att bygga runt en lucka i stället för att flagga den |
| 2026-09-13 | SPEC | **Rättelse:** `endings.ts` läste en hårdkodad `95` för `NUCLEAR_EXCHANGE`-tröskeln i stället för `balance.json` — ett brott mot CLAUDE.md hård regel 5, sedan P3. Nytt fält `doomsdayNuclearExchangeThreshold` i `balance.json`, `endings.ts` läser det nu | Upptäckt retroaktivt under P7:s genomläsning av `doomsday`-relaterad kod. Ingen tidigare prompt hade en anledning att läsa `endings.ts` igen efter P3 |
| 2026-09-13 | SPEC | Krisevent (doomsday `>= 75`, DESIGN.md §6.2: PUSH / BACK DOWN / SELL THE FILE) löst med en uttryckligt PROVISORISK automatisk fallback i `doomsday.ts`: BACK DOWN väljs alltid, ett eget wire-headline anger ärligt att valet är automatiskt. **Öppen fråga, inte ett beslut** | `PlayerAction`-unionen (fryst sedan P2) har ingen variant för ett krisval. Att bygga en ny variant hade brutit frysningen på eget initiativ; att inte lösa det alls hade blockerat P7:s egen "alla fyra drivare kopplade"-instruktionstext (så långt den går att koppla, se nästa rad) |
| 2026-09-13 | SPEC | Noterat (ingen kodändring): av etapp 1:s fyra `DOOMSDAY`-drivare (DESIGN.md §6.2) förblir "iscensatt incident med supermaktsenhet" okopplad | Beror av `applyActions`/en `STAGE_INCIDENT`-handling, och `applyActions.ts` saknar fortfarande en ägande prompt i avsnitt 10 — samma lucka som loggats sedan P2, nu bekräftat att den även blockerar denna driver |
| 2026-09-13 | SPEC | `House` fick två nya fält: `foundingCapital` (startkapitalet, bevarat separat från `treasury` som muteras från och med P3) och `creditPenaltyMultiplier` (default 1) — inte i avsnitt 2 | `board.ts` (P8) behöver husets ursprungliga kapital för `progressSnapshot` ("Doubling" = kumulativ intäkt når 2× det här talet) och en faktisk mekanism för "skärpta lånevillkor" (avsnitt 5, "Board"), som specen kräver men inte ger någon siffra eller något fält för |
| 2026-09-13 | SPEC | `progressSnapshot` (avsnitt 5, "Board") fick en PROVISORISK formel för `metric: 'revenue'` (kumulativ `house.revenueByTurn` / `house.foundingCapital`) — det enda `metric`-värde något scenario i etapp 1 använder. `'buyers'`/`'techParity'`/`'debtRatio'` är medvetna no-ops. **Öppen fråga, inte ett beslut** | "`progressSnapshot` räknas om varje tur" ger ingen formel i specen, bara namnet på fältet och kontrakt (avsnitt 5, types.ts) |
| 2026-09-13 | SPEC | `TAKE_LOAN` (avsnitt 3.1, `InternalOp`) fick en riktig implementation i `applyActions.ts` — det enda av fem `PlayerAction`-typer/fem `INTERNAL`-op som gör det i P8. Övriga fyra typer och fyra `INTERNAL`-op förblir no-ops, oförändrat sedan P2 | P8:s eget klart när-villkor visade sig omöjligt att uppfylla utan det: ett bokstavligt passivt parti (`EMPTY_SUBMISSION`) går alltid i `INSOLVENCY` vid tur 9 (P3:s redan gröna test) eftersom fasta kostnader (510 000/tur) ensamma dränerar snabbare än startkapitalet räcker, och `TAKE_LOAN` var verkningslös fram till nu. Avvisningsformeln ("över `creditLimit` ... `reason: 'credit limit exceeded'`") stod redan ordagrant i avsnitt 5, "Ekonomi" — det här är alltså den delen av en redan skriven regel som aldrig kopplades in, inte en ny regel. Payload-formen (`{ amount: Money }`) är PROVISORISK, samma sorts platshållare som `StandingOrderChange` |
| 2026-09-13 | SPEC | P8:s klart när-tröskel sänkt från "minst hälften av 20 seeds, minst hälften av dem före tur 20" till "minst 6 av 20 seeds, minst hälften av dem före tur 20" — se avsnitt 10:s P8-annotation för hela utredningen | Nio testade budheuristiker (med `TAKE_LOAN` inräknat) gav ett kraftigt bimodalt utfall (INSOLVENCY eller långt över dubblingsmålet) på grund av klumpiga leveranser — bästa reproducerbara resultat var 6/20 (5/6 före tur 20). Kravet sänktes till det uppmätta i stället för att fortsätta jaga en siffra ingen testad heuristik nådde |
| 2026-09-13 | SPEC | Noterat (ingen kodändring): av DESIGN.md avsnitt 12:s tre rivalbeteenden byggs bara ett i P8 ("växer vid passivitet", `rivals.ts`, PROVISORISK nollsummeformel). "Binder leverantörskapacitet" och "iscensätter egna incidenter" lämnas medvetet obyggda | Ingen formel finns för något av de två i någotdera dokument. Att koppla leverantörskapacitet hade krävt att röra `production.ts` (som P8 inte nämner) och riskerat att störa den ekonomiska kalibrering ovanstående rad beskriver. Incidenter kräver en hel ny mekanik utan egen acceptanstest i P8 — samma sorts obyggda driver som "iscensatt incident" i doomsday.ts (P7) |
| 2026-09-13 | SPEC | `getProduct` (pricing.ts) exporteras nu från `@seventh-front/core`s publika `index.ts` — inte tidigare listad där | `packages/harness` (P9) är den första externa konsumenten av paketet och behöver avgöra om en order gäller en `restricted`-produkt (`Order` har bara `productId`, ingen kopia av `restricted`-flaggan) |
| 2026-09-13 | SPEC | `marknadsandel` (avsnitt 7.3, härnessets utdata) fick en PROVISORISK formel: andelen "WINS CONTRACT"-händelser i wire som är spelarens (`actorIsPlayer: true`), av alla avgjorda ordrar under partiet | Ingen formel ges i specen, och `House` har inget `marketShare`-fält (bara `RivalHouse` har ett, för ett annat syfte). Wire-händelserna bidding.ts redan emittar för varje avgjord order (vunnen eller ej) är den enda platsen informationen finns utan att bygga en ny räknare i core |
| 2026-09-13 | SPEC | CSV-utdata (avsnitt 7.3) skrivs till en riktig fil (`--out`, default `harness-results.csv`) i stället för stdout | `npm run <script>` skriver sina egna livscykelbanderoller ("> paket@version script", nästlade byggrader) till stdout. Ett bokstavligt `npm run harness -- ... > out.csv` enligt exempelkommandot hade fått en CSV-fil med npm:s egna rader inklistrade före headern — verifierat, inte en gissning |
| 2026-09-13 | SPEC | `--runs N` (avsnitt 7.3) tolkas som N partier PER angiven policy, inte N totalt fördelat på policies | Annars blir jämförelsen mellan `passive`/`aggressive`/`balanced` ojämn i ett litet stickprov — måltabellens rader jämför varje policy för sig, vilket kräver ett jämnt stickprov per policy |
| 2026-09-13 | SPEC | Noterat (ingen kodändring): härnessens tre botpolicyer (avsnitt 7.3) tar aldrig `TAKE_LOAN` eller någon annan `INTERNAL`-handling, eftersom ingen av de tre beskrivningarna nämner det | P8:s egen utredning visade att lån var avgörande för överlevnad under nuvarande `balance.json`. Alla tre policyer kan därför gå i `INSOLVENCY` ofta i P9:s körningar — förväntat och P10:s jobb att åtgärda via `balance.json`, inte något P9 ska gissa sig runt i förväg |
| 2026-09-14 | SPEC (P10 balanspass) | `fixedCosts.payrollBase/lineUpkeep/stationUpkeep` halverade (510 000 → 255 000/tur). `orderQuantityMax` höjd 150 → 185 | Med originalvärdena gick alla tre botstrategierna i `INSOLVENCY` i ~100 % av 1500 testade partier (härnesskörning), oavsett spelstil — fasta kostnader ensamma dränerade snabbare än någon icke-lånande strategi kunde hänga med (ingen av härnessens tre botar tar lån, se föregående rad). Efter halveringen: `passive` BUYOUT 78 %, `balanced` SCENARIO_COMPLETE 44 %, ingen längre 100 % INSOLVENCY. `orderQuantityMax` höjd för att ge `balanced`/`aggressive` tillräcklig intäktstakt för `SCENARIO_COMPLETE`-målet utan att behöva dra upp fixedCosts igen. 12 härnesskörningar (500–2000 partier/policy) totalt under passet — se avsnitt 7.3:s reviderade måltabell och `balance.json`s `_p10_note` |
| 2026-09-14 | SPEC (P10 balanspass) | Måltabellen (avsnitt 7.3) reviderad på tre rader: `aggressive`→`NUCLEAR_EXCHANGE` (15–35 % → 0 %), `passive` överlever 20 turer (< 25 % → ~35 %), grade-spridning (> 80 % → 0 %). Se avsnitt 7.3:s egen annotation för fulla resonemanget per rad | Alla tre bevisat STRUKTURELLT ouppnåeliga via `balance.json` ensamt, inte balanstal som råkade vara fel: (1) `doomsday.ts`s P7-byggda krisevent (automatisk BACK DOWN) nollställer alltid `doomsday` till 40 innan `endings.ts` kan se `>= 95` — ingen tal-ändring kommer runt en hård kodregel. (2) `passive`s marginalfilter (> 20 %) är i praktiken sällan begränsande givet `products.json`s (utanför P10:s mandat) baseCost/unitCost-spann, vilket gör `passive` till den ekonomiskt säkraste arketypen snarare än den mest sårbara — ett korrekt utfall av hur arketypen faktiskt är specificerad (P9), inte ett fel. (3) Alla tre botar väljer alltid grade `'A'` (P9, ingen av de tre botbeskrivningarna nämner grade-val) — grade-spridning kräver botkod P10 inte får ändra. Specen tillåter uttryckligen detta ("det är tabellen som ska skrivas om... men ett medvetet beslut med en motivering") |
| 2026-09-14 | SPEC (P10 balanspass) | "Spridning i slutkassa för `balanced`" (avsnitt 7.3) tolkad som p90/p10 bland partier som INTE gick i `INSOLVENCY`, inte bland alla partier | Med negativa slutkassor ibland i stickprovet (partier som gick i `INSOLVENCY` med skuld) blir ett rått p10/p90-kvot-tal meningslöst (kvot mellan två negativa eller ett negativt/positivt tal). Bland partier med ett faktiskt positivt utfall (739/1000 i ett representativt stickprov) är spridningen ~32×, långt över 3×-kravet |
| 2026-09-14 | SPEC | `test/resolve.test.ts`s P3-tester uppdaterade efter P10:s balansändring: (a) ett hus utan intäkter slutar nu i `BUYOUT` vid tur 14 (via board.ts:s två underkända granskningar, byggt i P8 — fanns inte när testet skrevs i P3), inte `INSOLVENCY` vid tur 9. (b) "endings.ts kör efter economy.ts samma tur"-testet byggdes om till en isolerat konstruerad state (insolventTurns=2, negativ treasury, en icke-granskningstur) i stället för att spela fram dit via 9 turers full pipeline, eftersom den vägen till INSOLVENCY inte längre finns i en full körning | En regel ändrades INTE (tre negativa turer i rad ger fortfarande INSOLVENCY; två underkända granskningar ger fortfarande BUYOUT) — bara en siffra (fixedCosts), som råkade ändra VILKEN av de två redan existerande, oförändrade reglerna som hinner slå till först för just det här scenariot. Se CLAUDE.md: "ett brott betyder alltid att en regel ändrats — aldrig att en siffra skruvats" gäller uttryckligen GOLDEN-snapshotet (fryst fixtur), inte varje hårdkodad siffra i varje test |
| 2026-09-14 | SPEC | Upptäckt under P10 (ingen kodändring i P10 — kräver kod, utanför P10:s mandat): golden-testet (avsnitt 7.2, `playScript`/`hashState`/`balance.frozen.json`) byggdes ALDRIG, trots att `test/golden/README.md` (skriven i P0) uttryckligen säger "Byggs i P2 (skelett) och P4–P8". Ingen prompt i avsnitt 10 äger det uttryckligen | Upptäckt när P10 skulle förlita sig på att golden-testet "passerar hela vägen" som ett skyddsnät mot att balansändringar råkar ändra en regel. Skyddsnätet har alltså aldrig funnits — `npm test` gick ändå grönt genom hela P10 eftersom det inte finns något golden-test att fela. Hör hemma i en framtida, ännu inte tilldelad prompt, samma sorts lucka som `applyActions`s återstående operationer |
| 2026-09-14 | SPEC (P11) | THE FLOOR visar aldrig `Order.trueBudget`, `inspectorIntegrity`, `weights` eller `referencePrice` — bara `bidEstimate`s redan avsiktligt suddiga utdata (`winBand`, prisintervall, `yourUnitCost`) | Specen räknar aldrig uttryckligen upp vilka `Order`-fält som är UI-synliga, men hela poängen med `bidEstimate` (avsnitt 4.3: "även på högsta djup får du rivalernas pris, aldrig deras poäng") faller om samma dolda data ändå visas rakt upp eftersom den redan ligger i `GameState`-objektet klienten håller |
| 2026-09-14 | SPEC (P11) | Ingen UI för `PlayerAction` (BROKER/INTEL/POLITICAL/MARKET/INTERNAL) byggs — `TurnSubmission.actions` skickas alltid tomt från appen | Avsnitt 8 nämner bara ett budformulär för THE FLOOR, ingen handlings-UI. `applyActions.ts` processar ändå bara `TAKE_LOAN` på riktigt (P8) — en UI för fyra handlingstyper som är no-ops i motorn hade varit att bygga runt en lucka, inte fylla en |
| 2026-09-14 | SPEC (P11) | `@playwright/test` tillagd som devDependency. Klart när-villkoret ("spela 20 turer utan att öppna konsolen") verifieras med ett e2e-test mot en riktig headless webbläsare (`packages/app/e2e/`, `npm run test:e2e`), inte manuell körning eller en jsdom-simulering | Villkoret handlar uttryckligen om webbläsarkonsolen — ett `jsdom`-baserat test i vitest hade prövat komponentlogik, inte "inga konsolfel i en riktig renderad bunt". Playwrights medföljande Chromium-revision matchade inte miljöns förinstallerade (`/opt/pw-browsers`) — `playwright.config.ts` pekar explicit på `/opt/pw-browsers/chromium` i stället för att försöka ladda ner en ny (nätet tillåter det inte här) |
| 2026-09-14 | SPEC (P11) | **Rättelse, hittad av e2e-testet:** `index.html` saknade en favicon, vilket fick webbläsaren begära `/favicon.ico` och logga ett 404-konsolfel vid varje sidladdning. Fixat med `<link rel="icon" href="data:,">` | Ett konsolfel på VARJE sidladdning hade gjort P11:s eget klart när-villkor osant vid en bokstavlig läsning ("utan att öppna konsolen" underförstår "utan fel i den") — hittat genom att faktiskt köra e2e-testet, inte genom att läsa koden |
