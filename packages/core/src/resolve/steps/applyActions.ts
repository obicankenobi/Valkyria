// applyActions — spelarens handlingar och bud, i inskickad ordning (spec 3.1).
// No-op i P2 (implementeras i en ännu ej tilldelad prompt — se ANDRINGSLOGG.md). Se ETAPP1_TEKNISK_SPEC.md avsnitt 3.2, 10.
import type { ResolveStep } from '../index.js'

export const applyActions: ResolveStep = () => {}
