// fronts — frontresolve: materielkvot, moral, position, attribution (spec 5, 10).
// No-op i P2 (implementeras i P6). Se ETAPP1_TEKNISK_SPEC.md avsnitt 3.2, 10.
import type { ResolveStep } from '../index.js'

export const fronts: ResolveStep = () => {}
