// factions — faktionsekonomi, publicSupport, bankrutt (spec 5, 11).
// No-op i P2 (implementeras i P7). Se ETAPP1_TEKNISK_SPEC.md avsnitt 3.2, 10.
import type { ResolveStep } from '../index.js'

export const factions: ResolveStep = () => {}
